# Book Store Spring Boot API

So far its a simple crud api. Made with spring boot and has dependency for h2, mysql and validation. Consists of Repo, Model, Service, Controller layers and tests for each layer.   
